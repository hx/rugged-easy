require 'spec_helper'

describe Rugged::Easy do
  it 'has a version number' do
    expect(Rugged::Easy::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
